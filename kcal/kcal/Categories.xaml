<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:kcal;assembly=kcal"
             x:Class="kcal.Categories"
             Title="Categories list">

  <ContentPage.ToolbarItems>
    <ToolbarItem x:Name="btn_addcategory" Name="Add" />
  </ContentPage.ToolbarItems>

  
 <ListView x:Name="CategoriesList">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          
          <ViewCell.ContextActions>
            <MenuItem Text="Select" />
            <MenuItem Text="Edit" />
            <MenuItem Text="Delete" Clicked="cm_btn_delete"/>
          </ViewCell.ContextActions>

          <ViewCell.View>
            <StackLayout Padding="5, 5, 0, 5"
                         Orientation="Horizontal"
                         Spacing="15">
              <Label x:Name="lbl_ID" Text="{Binding ID}" />
              <Label x:Name="lbl_CName" Text="{Binding CName}"
                     FontAttributes="Bold" />
            </StackLayout>
          </ViewCell.View>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>


  
</ContentPage>